name: Actualizar Excel cada 10h
on:
  schedule:
    - cron: '0 */10 * * *'  # Cada 10 horas (UTC)

jobs:
  actualizar-excel:
    runs-on: ubuntu-latest
    steps:
      - name: Llamar al endpoint protegido
        run: |
          # TODO: cambiar el url
          curl -X POST https://poliplanner.fly.dev/sync/ci \
          -d "password=${{ secrets.ADMIN_PASSWORD }}"
